# コード解説書 ― 戎居 繁秀 ポートフォリオサイト

> **使い方**：採点や面接で質問されたときにこのファイルを見て、
> 自分の言葉で答えられるまで繰り返し読む。
> コードを写経するより「なぜこう書いたか」を語れることが目的。

---

## 0. このサイト全体の設計思想（最初に頭に入れる）

### なぜ Next.js を使ったか
- サマーインターンの課題指定技術だから。
- HTML だけのサイトと違い、**画像の自動最適化・SEO設定・パフォーマンス改善**が最初から揃っている。

### App Router とは何か
- Next.js 13 以降の新しいルーティング方式。
- `app/` フォルダの中に `page.tsx` を置くとそれがページになる。
- 昔の `pages/` フォルダを使う方式（Pages Router）とは別物。

### サーバーコンポーネントとクライアントコンポーネントの違い（最重要）

| 種類 | どこで動く | "use client" | いつ使う |
|------|-----------|-------------|---------|
| サーバーコンポーネント | サーバー側（Vercel） | 不要 | 表示するだけでいいとき |
| クライアントコンポーネント | ユーザーのブラウザ | **必要** | クリック・入力など動きがあるとき |

**このサイトでの使い分け**：
- `Header.tsx` → ハンバーガーメニューの開閉があるので **クライアント**
- `ContactSection.tsx` → フォームの入力・送信があるので **クライアント**
- それ以外（Hero・About・Skills・Projects・Footer）→ 表示するだけなので **サーバー**

**なぜこれが大事か**：  
クライアントコンポーネントにすると JavaScript がブラウザに送られてサイトが重くなる。  
必要な部品だけクライアントにすることで、サイト全体を軽く保てる。これが Next.js の設計思想。

---

## 1. ファイル構造と各ファイルの役割

```
app/
├── layout.tsx          ← 全ページ共通の「額縁」（ヘッダー・フッターを配置）
├── page.tsx            ← トップページの「絵」（各セクションを並べるだけ）
├── globals.css         ← サイト全体の CSS（Tailwind の設定）
└── components/
    ├── Header.tsx       ← 固定ナビゲーション ＋ ハンバーガーメニュー
    ├── Footer.tsx       ← コピーライト表示
    ├── HeroSection.tsx  ← トップ画像・名前・キャッチコピー
    ├── AboutSection.tsx ← 経歴・強み
    ├── SkillsSection.tsx← スキル一覧
    ├── ProjectsSection.tsx← 制作物カード
    └── ContactSection.tsx← お問い合わせフォーム
```

---

## 2. `layout.tsx` ― 全ページ共通の額縁

### コード全体の役割
- `<html>` と `<body>` タグを定義する唯一のファイル。
- `{children}` の部分に `page.tsx` の内容が差し込まれる。
- **Header と Footer はここに置く**ので、どのページに移動しても共通表示される。

### なぜここに metadata を書くか
```ts
export const metadata: Metadata = {
  title: "戎居 繁秀 | ポートフォリオ",
  description: "...",
  openGraph: { ... },
};
```
- **metadata オブジェクトを export するだけ**で Next.js が `<head>` タグを自動生成してくれる。
- `<head>` を自分で書かなくていい＝書き忘れや書き間違いが減る。
- `openGraph` は SNS（Twitter・LINE など）でシェアされたときのカード表示に使われる。

### なぜ `bg-white text-gray-900` を body に書くか
- サイト全体のデフォルト色をここで決めると、各セクションで繰り返さなくていい。

---

## 3. `page.tsx` ― トップページ本体

```tsx
export default function Home() {
  return (
    <>
      <HeroSection />
      <AboutSection />
      <SkillsSection />
      <ProjectsSection />
      <ContactSection />
    </>
  );
}
```

### なぜこんなにシンプルか
- 各セクションは別ファイル（コンポーネント）に分けてある。
- `page.tsx` は「順番に並べる」だけの役割。
- **セクションを1つ追加したいなら、ここに1行足すだけ**で済む。

### `<>` は何か
- React Fragment（フラグメント）。
- コンポーネントは要素を1つしか返せないルールがあるが、`<div>` で囲むと不要な HTML が増える。
- `<>` を使うと HTML を汚さずに複数要素を返せる。

---

## 4. `Header.tsx` ― ナビゲーション

### なぜ "use client" を付けたか
```ts
"use client";
import { useState } from "react";
```
- `useState` はブラウザの状態（ハンバーガーメニューが開いているか）を管理する。
- `useState` はクライアントコンポーネントでしか使えない。だから `"use client"` が必要。

### navLinks の設計
```ts
const navLinks = [
  { name: "About", href: "#about" },
  { name: "Skills", href: "#skills" },
  { name: "Projects", href: "#projects" },
  { name: "Contact", href: "#contact" },
];
```
- リンクを**配列**で定義して `map` で展開する。
- ナビのリンクを追加・削除するときは**この配列だけ触る**。
- PC用ナビとスマホ用ナビの両方に同じ配列を使っているので、片方だけ変更し忘れることがない。

### useState でハンバーガーメニューを制御する
```ts
const [isOpen, setIsOpen] = useState(false);
```
- `isOpen` = 「メニューが開いているか」を表す変数（true/false）。
- `setIsOpen` = その変数を更新する関数。
- `setIsOpen(!isOpen)` でボタンを押すたびに true↔false が切り替わる。
- 値が変わると React が自動で画面を再描画してくれる。

### レスポンシブ対応
```tsx
{/* PC用：スマホでは hidden（非表示）*/}
<nav className="hidden md:flex gap-8">

{/* スマホ用ハンバーガー：PCでは md:hidden（非表示）*/}
<button className="md:hidden ...">
```
- Tailwind の `md:` は「画面幅が768px以上のとき」という意味。
- 同じ HTML を2つ書いて、それぞれ条件で出し分けている。

### 固定ヘッダーの設定
```tsx
className="fixed top-0 left-0 w-full ... z-50"
```
- `fixed` でスクロールしてもヘッダーが上に固定される。
- `z-50` は重なり順（z-index）。他の要素より手前に表示されるよう高い値を設定。
- `bg-white/80 backdrop-blur` で半透明の磨りガラス風に。`/80` は透明度80%の意味。

---

## 5. `HeroSection.tsx` ― トップ画像エリア

### なぜ "use client" が**ない**か
- クリックや入力などの動きが一切ない。
- 表示するだけなのでサーバーコンポーネントのまま。
- JavaScript がブラウザに送られないのでページが軽くなる。

### next/image を使った理由
```tsx
import Image from "next/image";

<Image
  src="/profile.jpg"
  alt="戎居 繁秀のプロフィール画像"
  width={200}
  height={200}
  priority
/>
```
- 普通の `<img>` タグと違い、`next/image` は：
  - **自動でWebP形式に変換**する（ファイルサイズが小さい）
  - **画面サイズに合わせてリサイズ**する
  - **遅延読み込み**（最初に見えない画像は後から読む）をデフォルトでする
- `priority` を付けると「この画像は最初に見えるので先に読め」という指示になり、**LCP（表示速度の指標）が改善**する。

### `min-h-screen` を付けた理由
- `min-h-screen` = 最低でも画面の高さ（100vh）を確保する。
- ヒーローセクションが画面全体を使うことで、サイトとして見栄えが良くなる。

### `pt-16` を付けた理由
- Header は `fixed`（固定）なので、画面の上64px（`h-16` = 16 × 4px）を常に占有している。
- `pt-16` でセクションの上に同じ分だけ余白を取らないと、文字がヘッダーに隠れてしまう。

### h1 タグを1つだけ使った理由
- `<h1>` はページの中で最も重要な見出しを表すタグ。
- **SEO（検索エンジン最適化）**上、1ページに1つだけ使うのがルール。
- このサイトでは自分の名前をh1にした。

---

## 6. `AboutSection.tsx` ― 経歴・強み

### 型定義とデータを分けた理由
```ts
type Career = {
  period: string;
  description: string;
};

const careers: Career[] = [ ... ];
```
- **型（Type）**を定義すると、「このオブジェクトには period と description が必ず入る」とTypeScriptが保証してくれる。
- 間違ったプロパティ名を書くとエディタがすぐ教えてくれる。

### なぜデータを配列にするか（重要な設計思想）
```ts
const careers: Career[] = [
  { period: "2025〜現在", description: "..." },
  { period: "〜2025",    description: "..." },
];
```
- 経歴を増やすとき、**配列に1行足すだけ**でいい。
- 表示するコード（`map` の部分）は変えなくていい。
- **データと表示を分離する**という設計の基本。将来 Supabase（DB）からデータを取ってくるように変えても、表示側のコードはそのままで済む。

### `key` プロパティを付ける理由
```tsx
{careers.map((career) => (
  <li key={career.period}>
```
- `map` でリストを表示するとき、React は各要素を識別するために `key` が必要。
- `key` がないと「どの要素が変わったか」を React が判断できなくなり、画面の更新が非効率になる。
- **一意な値**（ここでは `period`）を key にする。

### `aria-hidden="true"` を付けた理由
```tsx
<span aria-hidden="true">▷</span>
```
- 画面読み上げソフト（スクリーンリーダー）に「この記号は読み上げなくていい」と伝える。
- 「▷（右向き三角）」はデザイン上の飾りなので、視覚障害者には意味がない音を読まれないようにする。

---

## 7. `SkillsSection.tsx` ― スキル一覧

### 3グループにした理由
- 「実務で使用」「このサイトで実装」「Web・マーケ経験」の3カテゴリに分けると、
  採点者が「この人はどのレベルのスキルを持っているか」を一目で理解できる。
- **スキルの背景・文脈**を示すことで、ただのリストより説得力が上がる。

### バッジ形式のデザインを選んだ理由
```tsx
<li className="px-3 py-1 bg-white border border-gray-200 rounded-full text-sm ...">
```
- `rounded-full` で角を完全に丸くしてバッジ（タグ）形式に。
- 一目でスキルの数が把握でき、追加・削除が視覚的にわかりやすい。

---

## 8. `ProjectsSection.tsx` ― 制作物

### `article` タグを使った理由
```tsx
<article key={project.title} className="...">
```
- `<div>` でも動くが、`<article>` は「それ単体で意味をなすコンテンツ」を表すHTMLの正式な意味付け。
- SEOと読みやすいコードのために、意味に合ったタグを選んだ。

### `siteUrl?:` の `?` の意味
```ts
type Project = {
  githubUrl: string;
  siteUrl?: string;  ← ? が付いている
};
```
- `?` は「あってもなくてもいい（任意）」の意味（TypeScript の Optional）。
- 表示側では `{project.siteUrl && <a ...>}` で「あるときだけ表示」できる。

### `target="_blank" rel="noopener noreferrer"` を付けた理由
```tsx
<a href="..." target="_blank" rel="noopener noreferrer">
```
- `target="_blank"` = リンクを新しいタブで開く。
- `rel="noopener noreferrer"` = セキュリティ対策。`_blank` を使うと開かれたページから元のページを操作できる脆弱性があり、それを防ぐ。セットで使うのがルール。

---

## 9. `ContactSection.tsx` ― お問い合わせフォーム

### なぜ "use client" が必要か
- フォームは「入力する」「送信ボタンを押す」というユーザー操作（イベント）を扱う。
- ブラウザ上でのイベントを扱うには `useState` が必要。
- `useState` はクライアントコンポーネントでしか動かない → `"use client"` が必要。

### フォームの状態を1つのオブジェクトにまとめた理由
```ts
const [form, setForm] = useState<FormState>({
  name: "",
  email: "",
  message: "",
});
```
- name・email・message を別々の `useState` にすることもできるが、
  1つのオブジェクトにまとめると管理しやすい。
- **更新**も `setForm((prev) => ({ ...prev, [e.target.name]: e.target.value }))` の1行で済む。

### `e.target.name` で汎用化した理由
```ts
function handleChange(e) {
  setForm((prev) => ({ ...prev, [e.target.name]: e.target.value }));
}
```
- `e.target.name` は変化した input の `name` 属性（例: `"email"`）を返す。
- `[e.target.name]: e.target.value` は「`name` の値をキーにして更新する」という意味（計算プロパティ名）。
- 3つのフィールドに**同じ関数**を使い回せる。関数が1つで済むのでコードが短くなる。

### 送信状態を4段階にした理由
```ts
type SubmitStatus = "idle" | "loading" | "success" | "error";
```
- `idle`（未送信）→ `loading`（送信中）→ `success`（成功）or `error`（失敗）の4つ。
- これで：
  - 送信中はボタンを `disabled` にして**二重送信を防ぐ**
  - 成功したら**フォームを感謝メッセージに切り替える**
  - 失敗したら**エラーメッセージを表示する**
  が、すべて1つの変数で制御できる。

### `e.preventDefault()` を呼ぶ理由
```ts
async function handleSubmit(e: React.FormEvent) {
  e.preventDefault();
```
- `<form>` はデフォルトでボタンを押すとページをリロード（再読み込み）する。
- `preventDefault()` でそのデフォルト動作をキャンセルして、自分で送信処理を書く。

### GitHub の SVG を外部ライブラリなしで直接書いた理由
- `react-icons` などのライブラリを使うとサイズが大きくなる。
- SVGパスは変わらないので、**一度書けばずっと使える**。
- 外部依存（ライブラリ）を減らすと、将来のバージョンアップでの壊れが少なくなる。

---

## 10. Tailwind CSS の使い方まとめ

### 基本の考え方
- クラス名（例: `flex`、`text-gray-600`）を HTML タグに書くだけでスタイルが当たる。
- CSS ファイルを別に作る必要がない。

### よく使うパターン

| やりたいこと | Tailwindのクラス | 意味 |
|------------|----------------|------|
| レスポンシブ切り替え | `hidden md:flex` | 768px未満は非表示、以上はflex |
| 縦中央寄せ | `flex items-center` | flexで縦方向の中央寄せ |
| 横中央寄せ | `mx-auto` | 左右の margin を自動で均等に |
| 横幅の上限 | `max-w-5xl` | 最大1024px（`5xl`）で止める |
| ホバー効果 | `hover:bg-gray-700` | マウスを乗せたときに背景色変更 |
| アニメーション | `transition-colors` | 色変化を滑らかに |
| グリッドレイアウト | `grid md:grid-cols-2` | PC以上で2列 |
| 要素間の余白 | `gap-6` | グリッド・フレックスの要素間隔 |

### なぜ Tailwind を選んだか
- Next.js の標準で最初から入っている。
- クラス名を書くだけなので CSS を別ファイルで管理する手間がない。
- `md:` などのプレフィックスで**レスポンシブ対応**が簡単にできる。

---

## 11. SEO対策でやったこと（layout.tsx）

### title タグ
- ブラウザのタブに表示される。Google の検索結果にも出る。
- 「誰のサイトか + 何のサイトか」をわかりやすく書く。

### description
- Google 検索結果のタイトル下に出る説明文。
- 160文字以内が推奨（それ以上は省略される）。

### openGraph（OGタグ）
- SNS（Twitter・LINE・Slack など）でURLをシェアしたときのカード表示に使われる。
- `title`・`description`・`images` を設定すると見栄えの良いカードになる。

### h1 を1つだけ使う
- 検索エンジンはh1を「このページで最も重要な情報」と判断する。
- 複数書くとどれが重要かわからなくなるため、**1ページに1つ**が原則。

---

## 12. 採点者に聞かれそうな質問と回答

### Q. なぜ Next.js を選んだか
「サマーインターン課題の指定技術だったことが一番の理由です。ただ使うだけでなく、`next/image` による画像最適化やメタデータAPIによるSEO設定など、Next.js の強みを実際に体験することを意識しました。」

### Q. App Router とは何か
「Next.js 13以降の新しいルーティング方式で、`app/` フォルダにファイルを置くとそれがページになります。旧来の `pages/` フォルダを使う方式（Pages Router）とは設計思想が異なり、サーバーコンポーネントをデフォルトとした構成になっています。」

### Q. サーバーコンポーネントとクライアントコンポーネントをどう使い分けたか
「`"use client"` が必要なのは `useState` や イベントハンドラ（onClick など）を使う部品だけです。このサイトでは、ハンバーガーメニューを持つ Header と、フォーム入力を扱う ContactSection だけをクライアントにして、それ以外は全てサーバーコンポーネントのままにしました。これで不要な JavaScript がブラウザに送られず、サイトを軽く保てます。」

### Q. なぜ `next/image` を使ったか
「通常の `<img>` タグと違い、`next/image` は自動でWebP変換・リサイズ・遅延読み込みをしてくれます。また `priority` を付けることで最初に表示される画像を先読みし、LCP（Largest Contentful Paint）という表示速度の指標を改善できます。」

### Q. レスポンシブ対応はどう実現したか
「Tailwind CSS の `md:` プレフィックスを使いました。例えば `hidden md:flex` は『768px未満では非表示、以上ではflexで表示』という意味です。CSS のメディアクエリを直接書かなくても、クラス名の組み合わせだけでスマホ・タブレット・PCの出し分けができます。」

### Q. なぜデータを配列で定義したか（Projects・Skills・About）
「将来 Supabase（データベース）からデータを取得する形に変えることを想定しています。データを配列で定義して `map` で表示する構造にしておくと、データの出どころが変わっても表示側のコードは変えずに済みます。これがデータと表示を分離する設計の考え方です。」

### Q. TypeScript を使った理由は
「型を定義することで、たとえばプロジェクトカードに `title` が入っているかどうかをエディタがチェックしてくれます。実行する前にミスが見つかるので、バグが減りコードの意図が明確になります。Next.js の標準言語でもあります。」

### Q. SEO 対策で何をしたか
「`layout.tsx` に Next.js のメタデータAPIで `title`・`description`・`openGraph` を設定しました。メタデータAPIは、設定するだけで Next.js が `<head>` タグを自動生成してくれます。また `h1` タグをページ内に1つだけ（名前に）使い、検索エンジンに重要情報を伝えています。」

### Q. `e.preventDefault()` は何をしているか
「`<form>` 要素はボタンを押したときにデフォルトでページをリロードする動作があります。`preventDefault()` でそれをキャンセルして、自分で書いた送信処理（Supabaseへの保存など）を実行するためです。」

### Q. 今後改善したいことは
「現在フォームは送信の見た目だけで、実際には Supabase にデータが保存されていません。`npm install @supabase/supabase-js` をして Supabase クライアントを設定すれば、フォームのデータを DB に保存できるようになります。またプロフィール画像（`public/profile.jpg`）の配置も残っています。」

---

## 13. 残っている作業（Step 15〜16）

- [ ] **profile.jpg を `public/` に配置**する（500×500px 推奨）
- [ ] **og-image.png を `public/` に配置**する（1200×630px、SNSカード用）
- [ ] **Step 15：Lighthouse でパフォーマンス確認**（スコア80点以上が目標）
- [ ] **Step 16：git push して Vercel に自動デプロイ**
  ```bash
  git push origin main
  ```
  → Vercel が自動でビルドして公開URLが更新される

---

*作成日：2026年5月29日*
