# コード解説書 ― 戎居 繁秀 ポートフォリオサイト

> **このドキュメントの目的**
> このサイトの「何を作るか（要件定義）」と「どう作ったか（コードの意味）」を、
> IT初心者でも順番に追えるようにまとめたものです。
> 採点・面接で質問されたとき、**自分の言葉で「なぜこう書いたか」を説明できる**ことがゴール。
> Next.js / React / TypeScript / Supabase を学ぶ教材としても使えるよう、用語にも解説を付けています。
>
> 困ったら最後の **「付録A：用語集」** を先に読むと理解が早いです。

---

# 目次

- 第1部：要件定義（このサイトは何を満たすべきか）
- 第2部：全体設計（最初に頭に入れる考え方）
- 第3部：ファイル構造と役割
- 第4部：各ファイルの詳細コード解説
- 第5部：Supabase連携の仕組み（フォーム→DB保存）
- 第6部：Tailwind CSS の使い方
- 第7部：SEO・SNSシェア対策
- 第8部：想定Q&A（面接・採点対策）
- 付録A：用語集（IT初心者向け）
- 付録B：残っている作業

---

# 第1部：要件定義

> 「要件定義」とは、**作る前に『何を満たせば完成か』を言葉で決めること**。
> ここがブレると、作っている途中で迷子になる。実務の開発でも一番最初にやる工程。

## 1-1. プロジェクトの背景・目的

- **誰のためか**：理学療法士からITエンジニアへ転身中の「戎居 繁秀」本人を紹介するため。
- **何のためか**：サマーインターンの課題。採用担当・面接官に対し、経歴・スキル・制作物を1ページで伝える。
- **裏の目的**：作るだけでなく、**実装の理由を自分で説明できるようになる**こと（学習）。

## 1-2. 機能要件（Functional Requirements＝「何ができるか」）

| ID | 要件 | 実装した場所 |
|----|------|------------|
| F-01 | 1ページで完結する構成（スクロールで全情報が見られる） | `app/page.tsx` |
| F-02 | ヒーロー：写真・名前・キャッチコピー・行動ボタン(CTA) | `HeroSection.tsx` |
| F-03 | 自己紹介・経歴・強みの提示 | `AboutSection.tsx` |
| F-04 | 保有スキルをカテゴリ別に一覧表示 | `SkillsSection.tsx` |
| F-05 | 制作物の紹介＋実習で取り組んだ業務一覧 | `ProjectsSection.tsx` |
| F-06 | ナビゲーション（各セクションへ移動・スマホ対応） | `Header.tsx` |
| F-07 | お問い合わせフォーム → **データベースに保存** | `ContactSection.tsx` + `actions.ts` |
| F-08 | SNSでシェアした時にカード（画像＋説明）が出る | `layout.tsx`（OGP設定） |

## 1-3. 非機能要件（Non-Functional＝「どんな品質か」）

| ID | 要件 | どう実現したか |
|----|------|--------------|
| NF-01 | レスポンシブ（スマホ・PC両対応） | Tailwind の `md:` プレフィックス |
| NF-02 | 表示が速い | `next/image` の最適化＋静的生成(SSG) |
| NF-03 | 検索エンジンに正しく伝わる(SEO) | メタデータAPI・`h1`を1つだけ |
| NF-04 | 後から修正・拡張しやすい(保守性) | 「データと表示の分離」＋TypeScriptの型 |
| NF-05 | 安全（鍵を漏らさない・不正データを防ぐ） | サーバー専用の鍵・RLS・サーバー側検証 |

## 1-4. 技術スタックと選定理由

| 技術 | 役割 | なぜ選んだか |
|------|------|------------|
| **Next.js 16**（App Router） | サイトの土台（フレームワーク） | インターン課題の指定。画像最適化・SEO・高速表示が標準装備 |
| **React 19** | 画面を「部品(コンポーネント)」で組み立てる | Next.js の中核。再利用しやすい |
| **TypeScript** | 型のあるJavaScript | 書く前にミスを検知でき、コードの意図が明確 |
| **Tailwind CSS v4** | デザイン（CSS） | クラス名を書くだけ。レスポンシブが簡単 |
| **Supabase** | データベース（問い合わせ保存） | 無料枠＋PostgreSQL＋認証/RLSが揃う |
| **Vercel** | 公開（ホスティング） | Next.js製。git pushで自動デプロイ |

---

# 第2部：全体設計（最初に頭に入れる考え方）

## 2-1. なぜ「素のHTML」ではなく Next.js なのか

素のHTMLでも見た目は作れる。でも Next.js を使うと、最初から次が手に入る：
- **画像の自動最適化**（軽くて速い）
- **SEO設定**（検索・SNS対応）
- **サーバーの機能**（フォーム送信をサーバー側で安全に処理）

## 2-2. App Router とは

- Next.js 13以降の新しい仕組み。**`app/` フォルダにファイルを置くと、それがそのままページや部品になる**。
- `app/page.tsx` → トップページ（`/`）になる。
- 昔の `pages/` 方式（Pages Router）とは別物。今は App Router が主流。

## 2-3. サーバーコンポーネント vs クライアントコンポーネント（最重要）

Next.js の部品は2種類ある。違いを理解することが一番大事。

| 種類 | どこで動く | `"use client"` | いつ使う |
|------|-----------|---------------|---------|
| **サーバーコンポーネント** | サーバー（Vercel）側 | 不要（デフォルト） | ただ表示するだけのとき |
| **クライアントコンポーネント** | ユーザーのブラウザ | **必要** | クリック・入力など「動き」があるとき |

**このサイトでの使い分け：**
- `Header.tsx` → ハンバーガーメニューの開閉（動き）がある → **クライアント**
- `ContactSection.tsx` → フォーム入力・送信（動き）がある → **クライアント**
- それ以外（Hero / About / Skills / Projects / Footer）→ 表示だけ → **サーバー**

**なぜ使い分けるのか：**
クライアントにすると、その部品のJavaScriptがブラウザにダウンロードされる＝重くなる。
**動きが必要な部品だけ**クライアントにすれば、サイト全体を軽く保てる。これがNext.jsの設計思想。

## 2-4. リクエストから表示までの流れ（ざっくり）

```
① ユーザーがURLを開く
② Vercel（サーバー）が、サーバーコンポーネントを事前にHTMLへ変換（ビルド時に生成済み＝SSG）
③ できあがったHTMLをブラウザに返す（だから表示が速い）
④ クリック等の「動き」が必要な部品だけ、ブラウザでJavaScriptが動き出す（＝ハイドレーション）
⑤ フォーム送信時は、ブラウザ → サーバー（Server Action）→ Supabase へデータが流れる
```

> **SSG（Static Site Generation）＝静的サイト生成**：アクセスのたびに作り直すのではなく、
> **公開前(ビルド時)に完成HTMLを作っておく**方式。速くて安定。このサイトの大半はSSG。

---

# 第3部：ファイル構造と役割

```
portfolio-site-project/
├── app/
│   ├── layout.tsx           ← 全ページ共通の「額縁」(ヘッダー/フッター/メタ情報)
│   ├── page.tsx             ← トップページ本体（各セクションを並べるだけ）
│   ├── globals.css          ← サイト全体のCSS（Tailwind読み込み）
│   ├── actions.ts           ← 【サーバー】フォーム保存処理(Server Action)
│   ├── lib/
│   │   └── supabase.ts      ← 【サーバー】Supabase接続クライアント
│   └── components/
│       ├── Header.tsx        ← 固定ナビ＋ハンバーガーメニュー（クライアント）
│       ├── Footer.tsx        ← コピーライト（サーバー）
│       ├── HeroSection.tsx   ← 写真・名前・キャッチ（サーバー）
│       ├── AboutSection.tsx  ← 自己紹介・経歴・強み（サーバー）
│       ├── SkillsSection.tsx ← スキル一覧（サーバー）
│       ├── ProjectsSection.tsx← 制作物＋実習業務（サーバー）
│       └── ContactSection.tsx← 問い合わせフォーム（クライアント）
├── public/                   ← 画像など静的ファイル（profile.jpg 等）
├── .env.local               ← 秘密の鍵（Gitに上げない）
└── next.config.ts           ← Next.js全体の設定
```

> **覚え方**：`layout.tsx` が「額縁」、`page.tsx` が「絵」、`components/` が「絵の部品」。

---

# 第4部：各ファイルの詳細コード解説

## 4-1. `layout.tsx` ― 全ページ共通の額縁

### 役割
- `<html>` と `<body>` を定義する**唯一**のファイル。
- `{children}` の場所に `page.tsx` の中身が差し込まれる。
- Header と Footer をここに置くので、全ページで共通表示される。

### メタデータ（SEO/SNS情報）をここに書く理由
```ts
export const metadata: Metadata = {
  metadataBase: new URL("https://portfolio-site-project-puce.vercel.app"),
  title: "戎居 繁秀 | ポートフォリオ",
  description: "...",
  openGraph: { /* SNSカード用の情報 */ },
};
```
- `metadata` という名前のオブジェクトを **export するだけ**で、Next.jsが `<head>` タグを自動生成。
- 自分で `<head>` を書かないので、書き忘れ・書き間違いが減る。
- `metadataBase`：OG画像など相対パス(`/og-image.png`)を**本番の絶対URLに変換する基準**。
  これが無いと本番でも `localhost` 基準になり、SNSカードの画像が出ない。
- `openGraph`：LINE・X(Twitter)・Slack でシェアした時のカード表示に使う（F-08）。

---

## 4-2. `page.tsx` ― トップページ本体

```tsx
export default function Home() {
  return (
    <>
      <HeroSection />
      <AboutSection />
      <SkillsSection />
      <ProjectsSection />
      <ContactSection />
    </>
  );
}
```

### なぜこんなにシンプルか
- 各セクションは別ファイル(部品)に分けてある。`page.tsx` は**順番に並べるだけ**。
- セクションを足したいときは、ここに1行追加するだけ。

### `<>` とは（React Fragment）
- 部品は「**要素を1つだけ返す**」ルールがある。複数並べるには囲む必要がある。
- `<div>` で囲むと無駄なHTMLが増える。`<>...</>` なら**余計なタグを足さずに**複数を返せる。

---

## 4-3. `Header.tsx` ― ナビゲーション（クライアント）

### `"use client"` が必要な理由
```tsx
"use client";
import { useState } from "react";
```
- `useState`（状態を持つ仕組み）は**クライアントコンポーネント専用**。
- ハンバーガーメニューの「開いている/閉じている」を覚えておくのに `useState` を使う → `"use client"` が必須。

### リンクを配列で定義する理由
```tsx
const navLinks = [
  { name: "About", href: "#about" },
  { name: "Skills", href: "#skills" },
  { name: "Projects", href: "#projects" },
  { name: "Contact", href: "#contact" },
];
```
- リンクを**配列**にして `map` で展開。追加・削除はこの配列だけ触ればよい。
- PC用ナビとスマホ用ナビで**同じ配列**を使うので、片方だけ直し忘れる事故が起きない。
- `href="#about"` は**ページ内ジャンプ**（`id="about"` の場所までスクロール）。別ページ移動ではない＝1ページ完結(F-01)を支える。

### 開閉の状態管理
```tsx
const [isOpen, setIsOpen] = useState(false);
// ...
onClick={() => setIsOpen(!isOpen)}
```
- `isOpen` =「開いているか」(true/false)。`setIsOpen` でそれを更新。
- `!isOpen` は反転。押すたび true↔false が切り替わる。
- 状態が変わると React が**自動で画面を描き直す**（手動でDOM操作しない）。

### レスポンシブ（NF-01）
```tsx
<nav className="hidden md:flex ...">      {/* PC用：スマホでは非表示 */}
<button className="md:hidden ...">        {/* スマホ用：PCでは非表示 */}
```
- `md:` =「画面幅768px以上のとき」。`hidden md:flex` =「狭い時は隠す/広い時は表示」。

### 固定ヘッダー
```tsx
className="fixed top-0 left-0 w-full ... z-50 bg-white/80 backdrop-blur"
```
- `fixed` でスクロールしても上に固定。`z-50` で他要素より手前(重なり順)。
- `bg-white/80` は不透明度80%の白、`backdrop-blur` で磨りガラス風。

---

## 4-4. `HeroSection.tsx` ― トップ画像エリア（サーバー）

### `"use client"` が**ない**理由
- クリック・入力が一切ない＝表示だけ → サーバーコンポーネントのまま。JSがブラウザに送られず軽い。

### `next/image` を使う理由（NF-02）
```tsx
import Image from "next/image";
<Image src="/profile.jpg" alt="..." width={200} height={200} priority />
```
- 普通の `<img>` と違い、`next/image` は自動で：
  - **WebPへ変換**（軽い画像形式）
  - **画面に合わせてリサイズ**
  - **遅延読み込み**（見えない画像は後回し）
- `priority` =「この画像は最初に見えるので先読みして」。**LCP（表示速度の指標）**が改善。
- `alt` は画像の説明文。画像が出ない時や読み上げソフト用＝アクセシビリティとSEOに効く。

### `min-h-screen` と `pt-16`
- `min-h-screen` =最低でも画面の高さを確保 → ヒーローが画面いっぱいで見栄え良い。
- `pt-16` =固定ヘッダー(高さ64px=`h-16`)の分だけ上に余白。無いと文字がヘッダーに隠れる。

### `h1` を1つだけにする理由（NF-03 / SEO）
- `<h1>` はページ最重要の見出し。検索エンジンは「ここが一番大事」と解釈。
- **1ページに1つ**が原則。このサイトでは「氏名」をh1にした。

---

## 4-5. `AboutSection.tsx` ― 自己紹介・経歴・強み（サーバー）

### 「型 + 配列 + map」のパターン（保守性 NF-04 の核）
```tsx
type Career = { period: string; description: string };

const careers: Career[] = [
  { period: "2026年1月〜現在", description: "..." },
  { period: "2025年10月〜2026年6月", description: "..." },
  // ...
];
```
- **型(Type)**＝「このデータには `period` と `description` が必ずある」とTypeScriptに宣言。
  間違ったキー名を書くとエディタが即エラーで教えてくれる。
- **配列+map**＝経歴を増やすときは**配列に1件足すだけ**。表示コードは触らない。
  これが「**データと表示の分離**」。将来DBから取得する形に変えても表示側はそのまま。

### `map` の `key` が必要な理由
```tsx
{careers.map((career) => (
  <li key={career.period}> ... </li>
))}
```
- React はリストの各要素を区別するため `key`(一意の値)が必要。
- 無いと「どれが変わったか」を判断できず、再描画が非効率になる。

### `aria-hidden="true"`（アクセシビリティ）
```tsx
<span aria-hidden="true">▷</span>
```
- 読み上げソフトに「この記号は読まなくていい」と伝える。`▷` は飾りなので無意味な読み上げを防ぐ。

### 自己紹介文
- 経歴・強みの上に、PT→ITのストーリーを2〜3文で配置（`max-w-3xl mx-auto text-center`で中央の読みやすい幅に）。

---

## 4-6. `SkillsSection.tsx` ― スキル一覧（サーバー）

### グループ分けの設計
```tsx
type SkillGroup = { title: string; skills: string[] };
const skillGroups: SkillGroup[] = [
  { title: "実務で使用", skills: [...] },
  { title: "このポートフォリオで実装", skills: [...] },
  { title: "AIエージェント活用で開発", skills: [...] },
  { title: "Web・マーケ経験", skills: [...] },
];
```
- スキルを**文脈ごとにグループ化**することで、「どのレベル・どんな経験のスキルか」が一目で伝わる。
- 特に「**AIエージェント活用で開発**」グループは、FastAPI/OpenAI API/Stripe等を
  「AIエージェントを使いながら実装した」と**正直にラベリング**＝誇張せず実力が伝わる。
- ここも「型+配列+map」。グループや項目を増やしても表示コードは不変。

---

## 4-7. `ProjectsSection.tsx` ― 制作物＋実習業務（サーバー）

### 制作物データの型（任意プロパティ `?`）
```tsx
type Project = {
  title: string;
  description: string;
  tags: string[];
  githubUrl?: string;  // ? = 任意（無くてもよい）
  siteUrl?: string;    // ? = 任意
};
```
- `?` は **Optional（任意）** の意味。GitHubリンクやデモURLは「ある制作物だけ」表示したい。
- 表示側で `{project.githubUrl && <a ...>}` と書けば「**あるときだけ表示**」できる。

### リンクの出し分け
```tsx
{(project.githubUrl || project.siteUrl) && (
  <div>
    {project.githubUrl && <a ...>GitHub →</a>}
    {project.siteUrl && <a ...>サイトを見る →</a>}
  </div>
)}
```
- `&&` は「左がtrueなら右を表示」。リンクが1つも無ければ枠ごと出さない。
- **ポートフォリオサイト自身への循環リンクは付けない**（今見ているサイトに飛ばすのは無意味）。
  一方 Reha Evidence の**実際に動くデモ**は価値があるので「サイトを見る」を付ける。

### `target="_blank"` と `rel="noopener noreferrer"`
```tsx
<a href="..." target="_blank" rel="noopener noreferrer">
```
- `target="_blank"` =新しいタブで開く（自分のサイトを離脱させない）。
- `rel="noopener noreferrer"` =**セキュリティ対策**。`_blank` で開いた先から元ページを操作される脆弱性を防ぐ。`_blank` とセットで必ず付ける。

### 「実習で取り組んだこと」リスト
```tsx
const internshipTasks: string[] = ["GEO・SEO記事作成", "お知らせ記事作成", ...];
// ...
<ul className="grid sm:grid-cols-2 ...">{internshipTasks.map(...)}</ul>
```
- 詳しい制作物（3件）とは別に、業務の**幅**を箇条書きで見せる。
- 8項目あるので `sm:grid-cols-2` でPCでは2列にしてコンパクトに。

---

## 4-8. `ContactSection.tsx` ― 問い合わせフォーム（クライアント）

### `"use client"` が必要な理由
- 入力・送信という**ユーザー操作(イベント)**を扱う。`useState` を使うので `"use client"` 必須。

### 状態を3つ持つ
```tsx
const [form, setForm] = useState<FormState>({ name: "", email: "", message: "" });
const [status, setStatus] = useState<SubmitStatus>("idle");
const [errorMessage, setErrorMessage] = useState("");
```
- `form` =入力値（3項目を1オブジェクトで管理）。
- `status` =送信状態（`"idle"|"loading"|"success"|"error"` の4段階）。
- `errorMessage` =サーバーが返した失敗理由。

### 1つの関数で3入力を扱う（汎用ハンドラ）
```tsx
function handleChange(e) {
  setForm((prev) => ({ ...prev, [e.target.name]: e.target.value }));
}
```
- `e.target.name` =変化したinputの `name`（例 `"email"`）。
- `[e.target.name]: e.target.value` =「そのキーだけ更新」（**計算プロパティ名**）。
- `...prev`（スプレッド構文）=他の値はそのままコピー。
- → name/email/message の3つに**同じ関数を使い回せる**。

### 送信処理（ここが今回の主役）
```tsx
async function handleSubmit(e) {
  e.preventDefault();          // ① フォーム標準のリロードを止める
  setStatus("loading");        // ② 送信中表示（ボタンをdisabledに）
  setErrorMessage("");

  const result = await submitContact(form);  // ③ サーバー(Server Action)を呼ぶ

  if (result.ok) {             // ④ 成功 → 感謝メッセージ＋入力クリア
    setStatus("success");
    setForm({ name: "", email: "", message: "" });
  } else {                     // ⑤ 失敗 → 理由を表示
    setErrorMessage(result.error);
    setStatus("error");
  }
}
```
- `e.preventDefault()`：`<form>` は標準でボタン押下時にページをリロードする。それを止めて自前処理にする。
- `status` が `"loading"` の間はボタンを `disabled` にして**二重送信を防止**。
- `submitContact` は**サーバーで動く関数**（第5部で詳説）。`await` で結果を待つ。

---

## 4-9. `app/lib/supabase.ts` ― Supabase接続（サーバー専用）

```tsx
import { createClient } from "@supabase/supabase-js";

export function createSupabaseServerClient() {
  const url = process.env.SUPABASE_URL;
  const anonKey = process.env.SUPABASE_ANON_KEY;
  if (!url || !anonKey) {
    throw new Error("Supabaseの環境変数が未設定です");
  }
  return createClient(url, anonKey);
}
```
- `process.env.XXX` =**環境変数**を読む（鍵などの秘密情報をコードに直書きしない）。
- 変数名に **`NEXT_PUBLIC_` を付けていない** → これらの値は**ブラウザに渡らずサーバーでだけ読める**。
  （`NEXT_PUBLIC_` を付けるとブラウザにも公開される。今回は付けない＝より安全）
- 鍵が無ければ即エラー＝設定漏れを早期に発見できる。

---

## 4-10. `app/actions.ts` ― Server Action（フォーム保存）

```tsx
"use server"; // このファイルの関数は全てサーバーで動く

import { createSupabaseServerClient } from "./lib/supabase";

export async function submitContact(input): Promise<SubmitResult> {
  // ① サーバー側でも入力を検証する
  const name = input.name?.trim();
  // ...（空チェック・メール形式・長さチェック）

  // ② Supabaseに1件追加
  const supabase = createSupabaseServerClient();
  const { error } = await supabase.from("contacts").insert({ name, email, message });

  if (error) return { ok: false, error: "送信に失敗しました…" };
  return { ok: true };
}
```
- `"use server"` =この関数は**サーバーで実行**される（ブラウザには送られない）。
- **なぜサーバー側でも検証するのか**：クライアントの `required` は開発者ツール等で回避できる。
  サーバー検証が「**最後の砦**」（NF-05 セキュリティの基本）。
- `supabase.from("contacts").insert({...})` =`contacts`テーブルに1行追加。
- 詳細エラーは `console.error`（サーバーログ）にだけ出し、利用者には**汎用メッセージ**を返す
  （DBの内部情報を外に漏らさない）。

---

# 第5部：Supabase連携の仕組み（フォーム → DB保存）

> ここが今回一番の学びどころ。**「ブラウザ → サーバー → データベース」**の流れを理解する。

## 5-1. 全体の流れ図

```
[ブラウザ] ContactSection（クライアント）
    │  handleSubmit で submitContact(form) を呼ぶ
    ▼
[サーバー] actions.ts の submitContact（"use server"）
    │  入力を再検証 → Supabaseクライアント生成
    ▼
[Supabase] contacts テーブルに insert
    │  RLS（行レベルセキュリティ）が「anonはINSERTのみ許可」を判定
    ▼
   保存完了 → { ok: true } をブラウザに返す → 「送信しました」表示
```

## 5-2. Server Action とは（Next.js 16の正式なやり方）

- **サーバーで動く関数を、クライアントから直接呼べる**仕組み。
- `"use server"` を付けた関数を import して `await` するだけ。裏側で自動的に
  ブラウザ→サーバーのPOST通信が行われる（通信コードを自分で書かなくていい）。
- なぜAPIルートでなくServer Action？ → コードが短く、型もそのまま渡せて、Next.js 16の標準だから。

## 5-3. RLS（Row Level Security）と GRANT ― 2段階の関門

Supabaseに安全に保存するため、データベース側で2つ設定した。

```sql
-- ① テーブル作成
create table public.contacts (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  email text not null,
  message text not null,
  created_at timestamptz not null default now()
);

-- ② RLSを有効化（デフォルトで全アクセス拒否になる）
alter table public.contacts enable row level security;

-- ③ 匿名ユーザー(anon)の INSERT だけを許可するポリシー
create policy "allow anonymous insert"
  on public.contacts for insert to anon with check (true);

-- ④ テーブル操作の権限(GRANT)を付与
grant insert on public.contacts to anon;
```

**RLS と GRANT の違い（今回ハマった重要ポイント）：**
- **GRANT** =「その役割が、そもそもINSERTを**試みてよいか**」というテーブルレベルの許可。
- **RLSポリシー** =「許可された操作のうち、**どの行**を対象にしてよいか」のルール。
- **両方そろって初めて**保存できる。GRANTを忘れると `permission denied`（今回これで一度失敗した）。

**セキュリティのポイント：**
- `INSERT`（追加）は許可するが `SELECT`（閲覧）は許可しない。
- → **誰でも問い合わせを送れるが、第三者が他人の問い合わせを読むことはできない**。
- 自分は Supabase ダッシュボードの Table Editor（管理者権限）で中身を確認する。

## 5-4. 環境変数の管理（.env.local と Vercel）

鍵(URL・anonキー)は**コードに直書きしない**。2か所に設定する。

| 場所 | 用途 | Gitに上がる？ |
|------|------|-------------|
| `.env.local`（ローカル） | 自分のPCでの開発・テスト用 | **上がらない**（`.gitignore` で除外） |
| Vercel の Environment Variables | 本番サイト用 | （Vercelが安全に保管） |

- どちらにも `SUPABASE_URL` と `SUPABASE_ANON_KEY` を設定。
- **`.env.local` をGitに上げない**のが鉄則（鍵が漏れると悪用される）。

---

# 第6部：Tailwind CSS の使い方

### 基本
- クラス名(`flex`, `text-gray-600` 等)をタグに書くだけでスタイルが当たる。別CSSファイル不要。

### よく使うパターン
| やりたいこと | クラス | 意味 |
|------------|-------|------|
| レスポンシブ切替 | `hidden md:flex` | 768px未満は非表示、以上はflex |
| 縦中央寄せ | `flex items-center` | 縦方向中央 |
| 横中央寄せ | `mx-auto` | 左右マージン自動で中央 |
| 横幅上限 | `max-w-5xl` / `max-w-3xl` | 最大幅を制限し読みやすく |
| ホバー | `hover:bg-gray-700` | マウスを乗せた時の変化 |
| 滑らかな変化 | `transition-colors` | 色変化をアニメーション |
| グリッド | `grid md:grid-cols-2` | PC以上で2列 |
| 余白 | `gap-6` / `py-24` / `px-4` | 間隔/上下余白/左右余白 |
| 箇条書き | `list-disc pl-5` | 黒点付きリスト |

---

# 第7部：SEO・SNSシェア対策（layout.tsx）

- **title**：ブラウザタブ＆検索結果のタイトル。「誰の・何のサイトか」を明記。
- **description**：検索結果の説明文（160文字以内推奨）。
- **openGraph**：SNSシェア時のカード（画像＋説明）。`metadataBase` で画像URLを本番基準に解決。
- **h1を1つだけ**：検索エンジンに「最重要情報＝氏名」と伝える。

---

# 第8部：想定Q&A（面接・採点対策）

### Q. なぜ Next.js を選んだ？
「インターン課題の指定でした。ただ使うだけでなく、`next/image` の画像最適化やメタデータAPIによるSEO、Server Actionによる安全なフォーム処理など、Next.jsの強みを実際に体験することを意識しました。」

### Q. サーバー/クライアントコンポーネントの使い分けは？
「`useState` やイベントを使う部品だけクライアントにします。このサイトでは Header とフォームの ContactSection だけクライアントで、他は全部サーバーコンポーネント。不要なJavaScriptをブラウザに送らず、サイトを軽く保つためです。」

### Q. フォームのデータはどう保存している？
「Server Action を使っています。ブラウザのフォームから `submitContact` というサーバー関数を呼び、サーバー側で入力を再検証してから Supabase の `contacts` テーブルに保存します。鍵は `NEXT_PUBLIC_` を付けずサーバーだけで読むので、ブラウザに漏れません。」

### Q. セキュリティで気をつけた点は？
「3つあります。①鍵をコードに直書きせず環境変数にし、ブラウザに渡らないようにした。②クライアントだけでなくサーバー側でも入力を検証した（クライアント検証は回避できるため）。③SupabaseのRLSで『anonはINSERTのみ、SELECT不可』にし、第三者が他人の問い合わせを読めないようにした。」

### Q. RLSとGRANTの違いは？
「GRANTは『その役割がINSERTを試みてよいか』というテーブル全体の権限、RLSは『どの行を操作してよいか』のルールです。両方必要で、実際GRANTを忘れて`permission denied`になり、`grant insert ... to anon`を追加して解決しました。」

### Q. なぜデータを配列で定義した？
「データと表示を分離するためです。経歴やスキルを配列にして `map` で表示すれば、項目を増やしても表示コードは変えなくて済みます。将来DB(Supabase)から取得する形に変えても、表示側はそのままで対応できます。」

### Q. TypeScriptの利点は？
「型を定義することで、必要なプロパティが揃っているかをエディタが実行前にチェックしてくれます。タイプミスや渡し忘れが減り、コードの意図も明確になります。」

### Q. レスポンシブはどう実現した？
「Tailwindの `md:` プレフィックスです。`hidden md:flex` なら『狭い画面では非表示、広い画面ではflex表示』。メディアクエリを書かずクラス名の組み合わせで出し分けできます。」

### Q. 表示速度のために何をした？
「大半をサーバーコンポーネント＋静的生成(SSG)にして事前にHTML化し、画像は `next/image` で自動最適化、最初の画像には `priority` を付けてLCPを改善しました。」

---

# 付録A：用語集（IT初心者向け）

| 用語 | かんたんな意味 |
|------|--------------|
| **フレームワーク** | アプリ開発の土台・ルールのセット（ここではNext.js） |
| **コンポーネント** | 画面の「部品」。ボタンやセクション1つ1つ |
| **サーバー** | サイトを配信する側のコンピュータ（Vercel） |
| **クライアント** | 利用者のブラウザ側 |
| **状態(state)** | 部品が覚えておく値（例：メニューが開いているか） |
| **useState** | 状態を作るReactの機能。クライアント専用 |
| **props** | 親の部品から子の部品へ渡すデータ |
| **型(Type)** | 「このデータはこんな形」という決まり。TypeScriptの中核 |
| **配列** | 値を順番に並べた入れ物 `[ ... ]` |
| **map** | 配列を1つずつ変換して並べる（リスト表示の定番） |
| **イベント** | クリック・入力などの操作 |
| **ハンドラ** | イベントが起きた時に動く関数（例：handleSubmit） |
| **SSG** | 公開前にHTMLを作っておく方式。速い |
| **メタデータ** | 検索/SNS用のページ情報（title等） |
| **OGP** | SNSシェア時のカード表示の仕組み |
| **環境変数** | 鍵などの秘密情報をコード外に置く仕組み（process.env） |
| **Server Action** | クライアントから呼べる「サーバーで動く関数」 |
| **RLS** | DBの行ごとのアクセス制御（行レベルセキュリティ） |
| **GRANT** | DBのテーブル操作権限の付与 |
| **デプロイ** | 作ったものを公開すること |
| **LCP** | ページの主要部分が表示されるまでの速度指標 |

---

# 付録B：残っている作業

- [x] プロフィール写真 `public/profile.jpg`（500×500）配置 → **完了**
- [x] Contactフォームの Supabase 保存（F-07）→ **完了・本番動作確認済み**
- [ ] **OG画像** `public/og-image.png`（1200×630）配置（F-08の画像。SNSカードのサムネ用）
- [ ] **Lighthouse** でパフォーマンス計測（目標80点以上）
- [ ] テスト用に入った `contacts` のダミー行を Supabase の Table Editor で削除

### 公開（デプロイ）の流れ（参考）
```bash
git push origin main   # → Vercel が自動でビルド＆公開
```
- 鍵（環境変数）はローカルの `.env.local` と Vercel の両方に必要。

---

*最終更新：2026年6月9日 ― Supabase連携(F-07)完了・要件定義/用語集を追加*
